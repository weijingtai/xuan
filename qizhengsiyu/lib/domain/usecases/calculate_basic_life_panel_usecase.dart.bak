import 'package:qizhengsiyu/domain/entities/models/base_panel_model.dart';
import 'package:qizhengsiyu/domain/entities/models/observer_position.dart';
import 'package:qizhengsiyu/domain/managers/hua_yao_manager.dart';
import 'package:qizhengsiyu/domain/managers/shen_sha_manager.dart';
import 'package:qizhengsiyu/domain/services/astro_service.dart';

import '../services/generate_base_panel_service.dart';

class CalculateBasicLifePanelUseCase {
  final GenerateBasePanelService generateBasePanelService;


  CalculateBasicLifePanelUseCase({
    required this.generateBasePanelService,
  });

  Future<BasePanelModel> execute(ObserverPosition observerPosition) async {
    final basePanelModel = await generateBasePanelService.calculate();


    // GenerateBasePanelService(
    // );

    // TODO: The following calculation logic needs to be implemented
    // final shenSha = await shenShaManager.calculate(...);
    // final huaYao = await huaYaoManager.calculate(...);

    return BasePanelModel(
      starAngleMapper: {}, // Replace with actual data
      shenShaMapper: {}, // Replace with actual data
      huaYaoMapper: {}, // Replace with actual data
      // ... other properties
    );
  }
}
