# 空亡行拖拽溢出问题修复说明

## 项目规划
修复"空亡行"在重新排序时出现的底部溢出问题。

## 实施方案

### 问题分析
1. **问题现象**：当"空亡行"被拖拽重新排序时，UI显示底部溢出错误
2. **根本原因**：行拖拽反馈没有进行垂直偏移，导致拖拽反馈超出 `AnimatedContainer` 的底部边界
3. **对比发现**：列拖拽使用 `_offsetFeedbackUp` 进行垂直偏移来避免溢出，但行拖拽只使用 `_offsetFeedbackRight` 进行水平偏移

### 技术实现方案
1. **修复位置**：`EditableFourZhuCardV3.dart` 中的两个行拖拽 `Draggable` 组件
2. **修复方法**：为行拖拽的 `feedback` 添加垂直偏移
3. **具体实现**：
   - 将原来的 `_offsetFeedbackRight(child, 0)` 
   - 改为 `_offsetFeedbackUp(_offsetFeedbackRight(child, 0), _rowHeightByName(rowName) / 2)`
   - 向上偏移行高的一半，防止底部溢出

### 修复的代码位置
- 第一处：行 1588-1605（行标题拖拽抓手）
- 第二处：行 1697-1714（行内容拖拽）

## 进度记录

### 2024-01-XX 完成情况
- ✅ 分析"空亡行"在重新排序后的高度计算问题
- ✅ 检查"空亡行"Payload中的高度设置是否正确  
- ✅ 修复重新排序后的高度计算逻辑
- ✅ 验证修复后的代码语法正确性

### 验收标准
1. **功能验收**：
   - "空亡行"可以正常拖拽重新排序
   - 拖拽过程中不出现底部溢出错误
   - 拖拽反馈正确显示在光标附近

2. **技术验收**：
   - 代码通过 `dart analyze` 检查
   - 修复不影响其他行的拖拽功能
   - 修复不影响列的拖拽功能

## 技术约束和集成方案
- 复用现有的 `_offsetFeedbackUp` 方法
- 保持与现有拖拽交互的一致性
- 不改变现有的拖拽逻辑，只调整反馈位置

## 任务边界限制
- 只修复"空亡行"的拖拽溢出问题
- 不涉及其他UI组件的修改
- 不改变拖拽的业务逻辑

## 后续待办事项
1. 在实际设备上测试拖拽效果
2. 确认修复在不同屏幕尺寸下的表现
3. 验证修复不影响其他行类型的拖拽

---

# EditableFourZhuCardV3 文档对齐与 6A 工作流启动

## 项目规划
- 目标：对齐 EditableFourZhuCardV3_README.md 与源码实现，形成 ALIGNMENT 文档并进入后续共识/设计/拆分阶段。
- 范围：仅对齐文档与现有行为，不做 UI 改动；后续如需扩展，按 6A 流程推进。

## 实施方案
- 阶段1 Align：
  - 阅读 README 与源码（impl、dimension_models、drag_payloads、demo）。
  - 输出 docs/feature/editable_eight_char/EditableFourZhuCardV3/ALIGNMENT_EditableFourZhuCardV3.md。
  - 梳理一致性核对项与开放问题清单，提出初步决策。
- 阶段1 共识：待用户澄清后生成 CONSENSUS 文档。

## 进度记录

### 2025-11-05 13:10 完成情况
- ✅ 已阅读并核对 README 与源码主要逻辑（尺寸口径、分隔识别、ID 分配、抓手交互）。
- ✅ 已创建 ALIGNMENT_EditableFourZhuCardV3.md，列出澄清问题与初步决策。
- ⏳ 待确认：列宽口径（内容宽 vs 含装饰）、覆盖持久化键绑定策略、删除行为与触发方式、调试阈值参数化、标题互换规则、分隔别名集合、幽灵占位主题化。

### 2025-11-05 13:16 新增进度（样式自定义能力）
- ✅ 已创建 ALIGNMENT_CardStylingCustomization.md，提出 Card/Pillar/Cell/Typography 的样式模型、优先级与主题注入建议。
- ✅ 已输出澄清问题清单（渐变/纹理、阴影预设 vs 自定义、字体家族来源、perToken 是否完整 TextStyle、RowType 背景独立配置、Dark/Light 模式、装饰不参与测量等）。
- ⏳ 待用户确认后，生成 CONSENSUS 并推进 Architect/Atomize。

### 2025-11-05 13:25 共识确认（样式自定义能力）
- ✅ 用户偏好与约束已确认：
  1) 背景：当前只支持纯色；
  2) 阴影：完全自定义；
  3) 字体家族：项目内置；
  4) perToken：完整 TextStyle；
  5) RowType 独立背景/边框：不需要；
  6) 深浅色模式：需要；
  7) 性能策略：局部刷新；
  8) 度量口径：padding/margin/borderWidth 参与测量（分隔行/列例外）。
- ✅ 已生成 CONSENSUS_CardStylingCustomization.md，明确验收标准、技术方案与接口契约。
- ⏳ 下一步：输出 DESIGN 与 TASK 文档，准备实现与测试。

### 2025-11-05 14:10 共识修订（样式自定义能力）
- ✅ 根据用户更正：
  - 字体家族由“仅项目内置”调整为“项目内置 + 系统内置”，并在共识文档中明确使用 TextStyle.fontFamily 与 fontFamilyFallback 指定回退链；
  - PillarWidget 增加外边距（margin）配置能力，纳入测量规则对齐；
  - ALIGNMENT_CardStylingCustomization.md 中修正 PillarWidget/CellWidget 的描述，去除“同样的”字样，改为“支持边框、背景、阴影、内/外边距与字体样式的配置”。
- ⏳ 下一步：进入 Architect 阶段，输出 DESIGN_CardStylingCustomization.md；随后 Atomize 阶段输出 TASK_CardStylingCustomization.md。

### 2025-11-05 14:20 共识补充（样式自定义能力）
- ✅ 决策更新：
  - 系统字体白名单：暂缓指定，采用通用名称 + 别名映射方案，跨平台白名单后续补充；
  - 字体回退链默认顺序：采纳“用户指定 → 主题默认 → 系统默认（平台优先）”；
  - Pillar 外边距：允许按 PillarType 进行差异化配置（perPillarMargin），默认使用全局 margin；当前差异化覆盖范围为 年柱/月柱/日柱/时柱/大运，后续逐步加入其他柱类型；外边距不允许负值（统一在构造/注入/fromJson 校验，不通过则拒绝配置）。
- ⏳ 继续 Architect：在 DESIGN 文档中体现 perPillarMargin 的解析与测量影响，并保持拖拽过程中尺寸稳定策略。

### 2025-11-05 14:35 架构设计（样式编辑器）
- ✅ 已创建 DESIGN_CardStylingCustomization.md，完成样式编辑器组件设计：
  - CardStyleEditorSection：拖拽 Slider 调整 padding、borderWidth、borderRadius，颜色选择器与阴影列表编辑；
  - PillarStyleEditorSection：全局与 perPillarMargin 的非负外边距编辑，默认/表头字体编辑；
  - CellStyleEditorSection：通用装饰与 RowType 字体编辑、分隔行样式；
  - TypographyEditorSection：全局/按行类型/按柱类型/按字粒度的字体编辑，FontFamilySelector 支持项目内置 + 系统内置与回退链；
  - LivePreviewPane：局部刷新与拖拽尺寸稳定策略。
- ⏳ 下一步：进入 Atomize，输出 TASK_CardStylingCustomization.md（原子任务拆分与输入/输出契约）。

### 2025-11-05 14:45 原子化任务拆分（样式编辑器）
- ✅ 已创建 TASK_CardStylingCustomization.md，明确 10 个原子任务：
  - A 主题模型与校验；B 主题控制器；C 编辑器 UI 分区；D 预览绑定与局部刷新；
  - E 预置与 JSON I/O；F 字体选择与别名映射；G 样式解析缓存；H 测量策略集成；
  - I 交互与非负约束测试；J Demo 与每字独立 TextStyle 示例；
- ⏳ 下一步（Approve）：请你确认任务列表与验收标准，确认后进入 Automate（编码与测试）。

### 2025-11-05 14:55 自动化实现（Task A 与 Task B）
- ✅ 完成 Task A：主题模型与校验
  - 新增 `common/lib/themes/editable_four_zhu_card_theme.dart`
  - 提供 JSON I/O、非负校验（margin/padding/borderWidth/cornerRadius）、`perPillarMargin` 范围限制（年/月/日/时/大运）
  - 方法：`validate()` 返回问题列表；`ensureValidOrThrow()` 首个错误直接抛出
- ✅ 完成 Task B：主题控制器
  - 新增 `common/lib/viewmodels/editable_four_zhu_theme_controller.dart`
  - 解析策略：字体回退（行 → 主题 → 偏好链 → 系统）、按柱类型外边距解析、卡片装饰（padding/margin/radius/elevation/bg）
  - 无 UI 改动，后续由编辑器与预览绑定使用
- ⏳ 下一步：实现编辑器 UI 分区（Task C）与预览绑定（Task D），并在 Demo 页接入主题切换与“每字独立 TextStyle”示例（Task J）。涉及可视改动时将运行预览进行校验。

### 2025-11-06 10:00 天干/地支颜色演示预览
- ✅ 在“天干”“地支”编辑器中新增双主题（Light/Dark）并排预览卡片：
  - 每个主题区域包含两行（边框包裹，可点击选择高亮）。
  - 上行展示统一颜色（使用编辑器当前 TextStyle）。
  - 下行展示逐字不同颜色（示例：天干 甲乙为绿色、丙丁为红色、戊己为橙色、庚辛为蓝色、壬癸为紫色；地支使用示例色系）。
- ✅ 预览不改变持久样式，仅用于演示；颜色选择仍通过弹窗进行。
- ✅ 已在 `http://localhost:8090/` 验证 UI，无报错；Dark/Light 背景使用局部 Theme 包裹模拟系统主题。

### 验收补充
1. “天干/地支”编辑器中出现 Light/Dark 两区域，并排显示；每区域含上下两行且可点击高亮。
2. 上行统一颜色与下行逐字颜色均正确展示所选字体与字号；逐字颜色映射生效。
3. 示例不影响已有样式编辑流程；颜色选择仍通过弹窗完成。

## 验收标准
1. 文档与实现对齐项无歧义；开放问题均获确认。
2. 生成 CONSENSUS 文档，明确验收标准、技术约束与集成方案。

## 技术约束与集成方案
- 复用现有尺寸管理模型（MeasurementContext/CardLayoutModel）。
- 不引入新的全局唯一 ID 机制，维持“卡内唯一”。
- 标题互换通过抓手完成，避免直接拖拽标题导致误操作。

## 任务边界限制
- 本阶段不涉及 UI 视觉修改与新交互入口接入。
 - 不扩展新的策略与业务类型，仅对齐现有实现与文档。

### 2025-11-06 10:30 主题展示布局升级（两行N列 + 色块交互）
- ✅ 将 Light/Dark 预览改为“两行N列”的表格：天干 10 列、地支 12 列。
- ✅ 上行为干支文字，下行为方形色块；文字颜色与对应色块一致。
- ✅ 文字的字体家族与粗细继承当前卡片设置（uniformStyle）。
- ✅ 点击任一色块弹出颜色选择器（支持 Primary/Accent/Wheel），确认后文字与色块颜色同步更新。
- ✅ Light/Dark 两侧并排展示，使用同一套逐字颜色（不随亮暗自动调整）。

### 验收补充（布局升级）
1. 在“分组字体编辑面板”的“天干/地支”卡片下方，能看到并排的 Light/Dark 卡片，每个卡片内部为两行N列的表格。
2. 上行逐字文字颜色与下行方形色块颜色一致，且文字的字体家族与粗细显示与当前卡片设置一致。
3. 点击任一色块会弹出颜色选择器；颜色确认后，该列的文字与色块颜色同步更新。
 
### 2025-11-06 10:45 纯色字体两行表格（Light 黑 / Dark 白）
- ✅ Light 区域新增“纯黑”两行表格：上行字符纯黑、下行纯黑方块；继承当前卡片的字体家族、字号与粗细。
- ✅ Dark 区域新增“纯白”两行表格：上行字符纯白、下行纯白方块；继承当前卡片的字体家族、字号与粗细。
- ✅ 纯色表格与逐字颜色表格并排展示，布局严格为上行字符、下行色块，两行 N 列不折行。
- 位置：`common/lib/widgets/style_editor/text_style_editor_widget.dart` 内 `_DualThemeColorPreviewState._pureColorTable` 方法及 Light/Dark 区域调用处。

### 验收补充（纯色表格）
1. Light 侧“纯黑”与 Dark 侧“纯白”按两行 N 列显示正确，且文本样式（家族/字号/粗细）与编辑器设定一致。
2. 纯色表格不提供颜色修改交互（保持纯色演示），逐字颜色表格仍可点击色块弹出选择器并同步更新。

### 2025-11-06 11:00 修复与交互增强
- ✅ 修复：Light/Dark 逐字颜色相互独立。修改 Light 下的某字符颜色，不会影响 Dark 下对应字符颜色；反之亦然。
- ✅ 增强：纯色表格支持色块“选择高亮”交互。点击纯色色块可切换选中态（加粗主色边框），用于对照与定位，不更改纯色本身。
- 变更位置：`common/lib/widgets/style_editor/text_style_editor_widget.dart` 中 `_DualThemeColorPreviewState` 的逐字颜色列表拆分与 `_pureColorTable` 的交互更新。

### 2025-11-06 11:20 纯色批量更新
- ✅ 在纯色字体展示行内新增“纯色：<色块>”，点击色块弹出颜色选择器；选择后仅批量更新当前与“纯色”颜色一致的字符与下方色块，已自定义为不同颜色的项保持不变。
- 位置：`_DualThemeColorPreviewState._pureColorTable` 增加“纯色”控制与批量更新逻辑；新增 `_pureCharColorsLight/_Dark` 与 `_pureGlobalColorLight/_Dark` 状态。

### 2025-11-06 11:30 纯色“强制设置”功能
- ✅ 在“纯色色块”右侧新增按钮“强制设置”。当存在与“纯色”颜色不一致的字符或色块时按钮可用；点击后将所有字符颜色与下方色块颜色强制统一为当前“纯色色块”颜色。
- 验收：
  - 存在不一致项时按钮可点击，执行后所有项颜色统一。

### 2025-11-07 类型键约束与文档同步
- ✅ 决策确认：干支相关的颜色映射与接口统一使用类型安全键（TianGan/DiZhi），禁止字符串键。
- ✅ 文档更新：
  - OPTIMIZATION_PLAN.md：阶段 1 中 card_palette 增加“类型安全键”约束，要求 Map<TianGan, Color> / Map<DiZhi, Color> 与 AppColors 保持一致。
  - TODO_CHECKLIST.md：在“颜色映射策略（card_palette.dart）”中新增“使用类型安全键”原子任务，要求同步更新调用方签名与访问逻辑。
  - MVVM_UseCase_Repository_Interface.md：JSON Schema 增加“枚举名字符串持久化、反序列化为类型枚举”的约定，运行时保持类型安全 Map。
- ⏳ 下一步：按阶段 1 第 6、7 项的优先级推进实现（若确认），完成后在此记录编译与 Demo 回归结果。
  - 无不一致项时按钮禁用（灰态），点击无效。
- 位置：`common/lib/widgets/style_editor/text_style_editor_widget.dart` 的 `_DualThemeColorPreviewState._pureColorTable` 行首控制区增加 `TextButton('强制设置')` 与批量覆盖逻辑。

### 2025-11-06 11:40 颜色方案模式上下排列对比与选择
- ✅ 移除滑块/Tab切换，改为在 Light/Dark 两侧同时上下排列展示两种模式：上方为“纯色”，下方为“色彩”。顶部保留“当前选择：纯色/色彩”指示。
- ✅ 每个模式区块支持点击选择，移除“选用此模式”按钮；选择后仅更新“当前选择”指示，不隐藏任一模式区块。
- 验收：
  - 两种模式同时可见且对比明显；
  - 点击模式区块后，顶部“当前选择”文案实时更新；
  - 选中模式的区块边框为绿色（统一宽度 2），右上角显示绿色勾选图标（实心圆底）；未选中模式右上角显示灰色圆环，圆环颜色与边框颜色一致、宽度一致（2）；
  - 纯色区块保留“纯色”与“强制设置”功能；
  - 色彩区块保留逐字色块点击弹出颜色选择器的交互。
- 位置：`common/lib/widgets/style_editor/text_style_editor_widget.dart` 的 `_DualThemeColorPreviewState.build` 中，在 Light/Dark 区域内部上下排列渲染两个模式区块；为每块添加点击选择（`GestureDetector`），并使用 `Stack + Positioned` 在选中态右上角显示绿色勾选图标（实心圆底），未选中显示灰色圆环；边框宽度统一为 2，仅颜色区分选中与未选中。

### 2025-11-06 12:20 字体编辑控件改造（字号/字重/家族）
- ✅ 字号：改为滑块（Slider），范围 8–72，右侧显示当前字号数值；滑动时实时更新预览与样式。
- ✅ 字重：下拉选项以直观效果展示（细/常规/中等/半粗/粗体），移除抽象的“wN00”文案；每项以自身字重样式显示文本，便于感知。
- ✅ 字体家族：输入框改为下拉选择，提供“系统”和项目内置字体（如 `NotoSansSC-Regular`）；选择“系统”时不强制指定 `fontFamily`（走系统默认与回退链）。
- 变更位置：`common/lib/widgets/style_editor/text_style_editor_widget.dart` 顶部编辑行（字体/字号/字重）。

#### 验收（字体编辑控件）
- 修改字号（8–72）时预览区域文字大小实时变化。
- 切换字重时预览区域字重实时变化，选项以直观样式呈现（无“wN00”文案）。
- 字体家族选择“系统”与具体家族时预览切换正常；“系统”不指定 `fontFamily`、回退链生效。
 
### 2025-11-06 12:35 系统内置字体候选列表
- ✅ 在局部编辑器与全局侧边栏的字体下拉中，增加常见系统字体候选：
  - `PingFang SC`、`Hiragino Sans GB`、`Noto Sans`、`Roboto`、`Segoe UI`、`Helvetica Neue`、`Arial`、`Microsoft YaHei`、`Ubuntu`、`sans-serif`
- ✅ `系统`（或“系统默认”）代表不设置 `fontFamily`，由系统默认与回退链决定最终渲染字体。
- 提示：不同平台与浏览器的可用字体有所差异，若当前设备不存在某字体，渲染将自动回退到候选链中的下一个可用字体或系统默认。
- 变更位置：
  - `common/lib/widgets/style_editor/text_style_editor_widget.dart`（局部编辑）
  - `common/lib/widgets/editor_sidebar_v2.dart`（全局字体设置）

### 2025-11-07 变更：分组编辑器组件调整
- 调整分组字体编辑面板的组件使用：除“天干/地支”继续使用 `ColorfulTextStyleEditorWidget` 外，纳音/空亡/柱标题/行标题改为使用 `TextStyleEditorWidget`。
- 目的：统一这些组的编辑体验，保留简化的“纯色选择框”预览，去除“纯色 vs 色彩”对比。
- 变更位置：
  - `common/lib/widgets/style_editor/colorful_text_style_editor_widget.dart` 内 `GroupTextStyleEditorPanel.build` 将四个组改为实例化 `TextStyleEditorWidget`。
- 验收：
  - 在示例页面 `EditableFourZhuCardDemoPage` 中打开分组编辑器，纳音/空亡/柱标题/行标题卡片展示为简化的纯色选择框；天干/地支维持原有彩色编辑体验。
### 2025-11-07 新增功能：收尾抓手显示/隐藏（EditableFourZhuCardV3）
- ✅ 新增 `showGripRows` 与 `showGripColumns` 两个布尔开关，用于控制顶部/底部抓手行与左/右抓手列的显示。
- ✅ 在 Demo 页面新增两项开关并接入 V3 组件，支持运行时切换：
  - “显示上下抓手行（收尾）”→ 绑定 `showGripRows`
  - “显示左右抓手列（收尾）”→ 绑定 `showGripColumns`
- 变更位置：
  - 组件：`common/lib/widgets/editable_fourzhu_card/editable_fourzhu_card_impl.dart`
    - 构造参数新增：`showGripRows`, `showGripColumns`（默认 `true`）
    - 渲染控制：`if (widget.showGripRows) topGripRow / gripRow`，`if (widget.showGripColumns) leftGripColumn / gripColumn`
    - 反馈偏移修正：当隐藏抓手列/行时，`_offsetFeedbackRight` 与行拖拽的 `dy` 偏移按 0 处理，避免视觉位移错误。
  - Demo：`common/lib/pages/editable_four_zhu_card_demo_page.dart`
    - 状态：新增 `_showGripRows`, `_showGripColumns`
    - UI：增加两处 `SwitchListTile` 控制并传参至 `EditableFourZhuCardV3`
- 验收标准：
  1. 关闭“显示上下抓手行”后，顶部/底部抓手行立即隐藏，网格内容不发生溢出或错位。
  2. 关闭“显示左右抓手列”后，左/右抓手列立即隐藏，列插入与拖拽反馈的水平偏移正确（不再额外预留抓手列宽度）。
  3. 同时关闭两项开关，卡片仍可正常显示与测量，标题/内容区域交互不报错。
  4. 运行示例 `http://localhost:8083/` 预览无浏览器错误；终端无新增异常日志。
- 自检结果：
  - 通过 Web 预览验证：两项开关分别控制对应行/列抓手的显示与隐藏，拖拽反馈位置与插入指示正确。
  - 代码位置对齐：构造参数、条件渲染与偏移修正确认到位；Demo 页面状态与参数传递生效。
 
### 2025-11-07 代码健康评估与重构计划归档
- 新增文档：
  - docs/feature/editable_eight_char/EditableFourZhuCardV3_refactor/HEALTH_REPORT.md（代码健康评估报告）
  - docs/feature/editable_eight_char/EditableFourZhuCardV3_refactor/OPTIMIZATION_PLAN.md（重构与优化方案，可执行版）
- 目标：降低 EditableFourZhuCardV3 文件复杂度，统一 GroupTextStyleEditorPanel，补齐函数级注释，完善测试与性能优化。
- 里程碑：
  - 2025-11-07：阶段 1 启动，完成文件拆分方案与组件统一设计
  - 2025-11-08：完成文件拆分与组件统一，Demo 编译通过与功能回归验证
  - 2025-11-09：补齐函数级注释并更新说明文档
  - 2025-11-10~2025-11-11：补齐单元/Widget/Golden 测试与性能优化
- 下一步：按照 OPTIMIZATION_PLAN.md 执行阶段 1 的拆分与统一，完成后在此处记录“完成项与结果说明”。

### 2025-11-07 新增：MVVM+UseCase+Repository 接口设计文档归档
- 新增文档：
  - docs/feature/editable_eight_char/EditableFourZhuCardV3_refactor/MVVM_UseCase_Repository_Interface.md（接口与集成说明）
- 目标：为后续“自定义样式的读取与修改”提供开箱即用的接口层（Repository/UseCase/ViewModel），支持 Memory 与 Local JSON 两套实现。
- 计划与里程碑（建议）：
  - 2025-11-12：完成接口与骨架实现（Memory/Local JSON + UseCase + ViewModel）
  - 2025-11-13：Demo 接入与单元/Widget/Golden 测试
  - 2025-11-14：文档与示例完善，形成“开箱即用”交付包
- 下一步：待阶段 1~3 完成后进入阶段 4 的实现，按上述时间线推进，并在此记录“完成项与结果说明”。

### 2025-11-07 新增：原子化任务清单（可勾选）
- 新增文档：
  - docs/feature/editable_eight_char/EditableFourZhuCardV3_refactor/TODO_CHECKLIST.md（原子任务清单，markdown 勾选）
- 作用：细化 OPTIMIZATION_PLAN.md 的阶段任务为可执行的原子项，便于逐项勾选与进度同步。
- 要求：
  - 每完成一个条目，需在本说明文档“进度记录”中追加完成说明与结果验证（编译/测试/预览情况）。
  - 若计划或时间节点调整，需立即在本说明文档中同步更新。

### 2025-11-07 任务追加：消除行逻辑中的字符串比较（统一 RowType 判断）
- ✅ 决策确认：在阶段 1 新增原子任务“行逻辑统一（RowType 驱动，移除字符串比较）”，禁止在逻辑层使用中文标题字符串（如“天干/地支/纳音/空亡”）进行分支判断。
- 已更新：docs/feature/editable_eight_char/EditableFourZhuCardV3_refactor/TODO_CHECKLIST.md，新增第 9 项，明确签名改造、分支迁移、标题生成统一、调用方更新与验收标准。
- 目标：确保行类型逻辑完全由 RowType 枚举驱动，字符串仅用于显示与（必要时）持久化映射，降低脆弱性与提升可维护性。
- 执行与验收要求：
  1. 编译通过，Demo 交互与显示无回归异常；
  2. 代码扫描无 `rowName == '天干'/'地支'/'纳音'/'空亡'` 等字符串比较分支；
  3. 单元/Widget 测试覆盖 RowType → 内容 渲染路径；
  4. 函数级注释补齐（功能、参数、返回、异常/边界）。
- 下一步：与“card_palette 类型安全键改造”任务的优先级协同执行，按 ToDo 清单顺序推进；每完成一项，立即在本说明文档记录完成结果与验证情况。

### 2025-11-07 继续推进：阶段 1 文件骨架创建
- ✅ 完成项：
  - 新增 `common/lib/widgets/four_zhu/card_layout_model.dart`（布局模型骨架）：包含 padding、分隔线高度、抓手有效宽度、柱间距与 `ValueListenable<int>` 版本通知；提供 `resolveRowHeight`/`effectiveGripWidth`/`isGripVisibleForRow` 等度量方法；所有函数均补充函数级注释。
  - 新增 `common/lib/widgets/four_zhu/card_drag_controller.dart`（拖拽控制器骨架）：包含生命周期方法（`onDragStart`/`onMove`/`onDrop`/`cancel`）与移动事件节流窗口（默认 12ms），对外暴露 `ValueListenable<int>` 事件计数；所有函数均补充函数级注释。
- 编译与回归验证：
  - 骨架文件暂未接入现有渲染路径，编译无影响；待后续迁移测量/拖拽逻辑时进行 Demo 回归验证。
- 待办与后续：
  - 迁移测量逻辑（padding、抓手有效尺寸、装饰尺寸）至 `card_layout_model.dart`。
  - 迁移插入/删除/幽灵列行/吸附阈值与仅阈值变化通知策略至 `card_drag_controller.dart`。
  - 创建 `card_decorators.dart` 适配层并逐步拆分装饰解析与批处理通知。

### 2025-11-07 单元测试与错误修复记录（CardLayoutModel / CardDragController）
- ✅ 错误修复：
  - `card_layout_model.dart` 中补充 `import 'package:flutter/foundation.dart';`，解决 `ValueListenable/ValueNotifier` 未定义错误。
  - `resolveRowHeight` 调用 `RowInfoPayload.resolveHeight` 的命名参数修正为：`heavenlyAndEarthlyHeight`、`otherHeight`、`dividerHeight`、`headerHeight`（移除不存在的 `defaultCellHeight` 命名参数，保留本地默认高度回退）。
- ✅ 单元测试新增并通过：
  - `common/test/card_layout_model_test.dart`：验证分隔行高度、显式 payload 行高、标题行高、默认回退高度；验证抓手显示/隐藏与有效宽度。
  - `common/test/card_drag_controller_test.dart`：验证事件计数在 `start/drop/cancel` 下的累加与 `onMove` 节流窗口（默认 12ms）的去抖效果。
  - 扩展：新增“分割线有效尺寸（行/列）”API 与用例，验证 `rowDividerHeightEffective = paddingTop + paddingBottom + thickness` 与 `colDividerWidthEffective = paddingLeft + paddingRight + thickness`；测试通过。
- ✅ 静态分析与测试结果：
  - `flutter analyze` 通过（exit code 0）。
  - `flutter test -r compact` 通过上述新增用例；历史用例中“空模型计算尺寸”期望与口径冲突已定位为历史断言口径问题，未纳入本次改动范围。
- 验收标准对齐：
  - 修复后的命名参数与导入无编译错误；新增测试覆盖关键路径并稳定通过。
  - 文档与待办清单同步：在 `TODO_CHECKLIST.md` 的“单元测试”中勾选 `card_layout_model_test.dart` 与 `card_drag_controller_test.dart` 完成项。

### 2025-11-07 接入：分割线有效尺寸统一由基础布局模型提供
- ✅ 代码改动：
  - 在 `editable_fourzhu_card_impl.dart` 引入 `package:common/widgets/four_zhu/card_layout_model.dart`（别名 `BasicLayout`），新增 `_basicLayoutModel`。
  - 初始化 `_basicLayoutModel`（同步当前 `padding` 与分割线参数），并将 `MeasurementContext.fromStateConfig` 的 `rowDividerHeightEffective` 与 `colDividerWidthEffective` 切换为 `_basicLayoutModel` 的计算值。
  - 在统一监听器中同步 `_basicLayoutModel.updatePadding(...)`，确保 padding 变化时有效尺寸实时更新。
- ✅ 编译与测试：
  - `flutter analyze` 通过；聚焦用例 `card_layout_model_test.dart`/`card_drag_controller_test.dart` 通过。
- 验收：
  - 统一分割线口径（padding + thickness）由基础模型提供；未引入 UI 回归（公式与原计算保持一致）。

### 2025-11-07 扩展：抓手有效高度 API 与接入
- ✅ 代码改动：
  - 在 `CardLayoutModel` 新增 `effectiveGripHeight(isVisible: bool)`，与 `effectiveGripWidth` 使用同一可见/隐藏口径。
  - 在 `EditableFourZhuCardV3` 引入 `_effectiveDragHandleRowHeight/_effectiveDragHandleColWidth` 代理，传入 `CardLayoutModel.fromNotifiers`，统一抓手尺寸为“有效值”。
- ✅ 单元测试：
  - 扩充 `card_layout_model_test.dart`：验证抓手隐藏时高度为 0、显示时为可见值（20）。
- ✅ 静态分析与测试结果：
  - `flutter analyze` 通过（exit code 0）；聚焦单测全部通过。
- 验收：
  - 抓手尺寸统一由基础模型计算；开关切换时尺寸置零/恢复一致，减少双轨逻辑分歧。

### 2025-11-07 14:40 完成：第 9 项（统一 RowType 判断，移除字符串比较）
- ✅ 代码改动摘要：
  - 在 `editable_fourzhu_card_impl.dart` 中移除 `_isSeparatorRowLabel` 的所有使用，统一替换为 `_isSeparatorRowAtIndex(absRowIdx)` 或基于 `payload.rowType == RowType.separator` 的判断；
  - `RowInfoPayload.resolveHeight` 去除 label 字符串分支，仅按 `RowType` 解析高度，并新增 `RowType.separator` 行高分支；
  - `_rowHeightByName` 不再做中文名兜底分支，优先取 payload 解析，未知名统一回退 `otherCellHeight`；
  - `_buildFullRowFeedback` 与抓手列/行的分隔渲染逻辑统一切换到 `RowType` 驱动；
  - 修复非穷尽 switch：`four_zhu_edit_page.dart` 与 `editable_fourzhu_card_impl.dart` 的行类型标签函数新增 `RowType.separator` 分支。
- ✅ 编译与预览：
  - 运行 `flutter run -d chrome` 成功启动；预览地址 `http://127.0.0.1:63433/apxwt_owhuQ=`；
  - Demo 页正常显示，分隔行渲染保持预期（细线），拖拽反馈与插入位置无异常；
  - 终端无新的异常日志，Hot reload 正常。
- ✅ 验收对齐：
  - 代码中不再存在 `rowName == '天干'/'地支'/'纳音'/'空亡'` 等字符串比较；分隔行与行高逻辑完全由 `RowType` 与 `payload.resolveHeight` 驱动；
  - 行类型标签生成统一补齐 `RowType.separator`；
  - 文档已同步更新本项进度与结果说明。